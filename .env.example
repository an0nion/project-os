# ── Anthropic (Tier 2/3 — Haiku/Sonnet/Opus) ─────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Google AI (Tier 1/2 — Gemini Flash-Lite free tier + Gemini Pro) ──────────
# Get key: https://aistudio.google.com/app/apikey (free, 1000 req/day on Flash)
GOOGLE_AI_API_KEY=AIza...

# ── DeepSeek (Tier 1 fallback — cheap, ~$0.14/M input) ───────────────────────
# Get key: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-...

# ── Supabase (free tier: 500MB, 50k rows, 500k edge calls) ───────────────────
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJ...anon-public-key...
SUPABASE_SERVICE_KEY=eyJ...service-role-key...   # server-side only

# ── Slack (free — socket mode, DM-only, 3 scopes) ────────────────────────────
# im:history  → read DMs to the bot
# im:write    → open DM conversations
# chat:write  → send messages
SLACK_BOT_TOKEN=xoxb-...
SLACK_APP_TOKEN=xapp-...
SLACK_USER_ID=U...          # your Slack user ID (for direct DM targeting)

# ── App ───────────────────────────────────────────────────────────────────────
APP_URL=http://localhost:3000
CRON_SECRET=dev-secret
APP_SECRET=choose-a-strong-password
# Separate secret for the /api/costs/log endpoint so a compromised VM bot
# (which only needs to log costs) cannot also call admin/session endpoints
# guarded by APP_SECRET. Generate with `openssl rand -hex 32`.
# During the rollout window the endpoint still accepts APP_SECRET with a
# deprecation warning; remove that fallback once the bot deploys are rotated.
COST_LOG_SECRET=
NODE_ENV=development                  # set to 'production' in deployed environment

# ── Google Calendar OAuth (for Slack bot reminder → calendar integration) ─────
# Create credentials at: https://console.cloud.google.com → APIs & Services → Credentials
# Enable: Google Calendar API. Scopes: calendar.events
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...
GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3000/api/calendar/callback

# Set to 'true' on the VM once calendar OAuth is connected (visit /api/calendar/auth)
# Leave 'false' to disable all calendar event creation (bot will still save to Kanban)
CALENDAR_ENABLED=false

# ── Web Push — VAPID keys (free, generate: npm run vapid) ────────────────────
NEXT_PUBLIC_VAPID_PUBLIC_KEY=BN...
VAPID_PRIVATE_KEY=...
VAPID_EMAIL=mailto:you@example.com

# ── Tavily (web search for the Slack bot — free tier 1000 req/month) ─────────
# Get key: https://app.tavily.com  (sign up → API Keys → New Key)
# Set on VM .env only — not needed in Vercel
TAVILY_API_KEY=tvly-...

# ── Telegram (optional secondary notifications — completely free) ─────────────
# TELEGRAM_BOT_TOKEN=123456:ABC-your-token
# TELEGRAM_CHAT_ID=your-numeric-chat-id

# ── Playwright on Vercel (optional — needed if you hit many JS-rendered pages) ─
# Uses @sparticuz/chromium — keep disabled until you need it
# ENABLE_PLAYWRIGHT=false
