# fly.toml — Slack bot (Socket Mode worker, no HTTP)
app = 'project-os-bot'
primary_region = 'syd'

[build]

[env]
  NODE_ENV = 'production'

# No [http_service] — pure worker process, no inbound traffic
[[vm]]
  size      = 'shared-cpu-1x'
  memory    = '256mb'
