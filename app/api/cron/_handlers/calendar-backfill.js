/**
 * Handler: calendar-backfill
 * Stub — Unit 1c will add the calendar-backfill logic.
 *
 * Invoked by `/api/cron/daily/route.js` and `/api/cron/run/[name]/route.js`.
 * Not an HTTP route — pure async function.
 */

export async function run() {
  console.log('[calendar-backfill] handler not yet wired — Unit 1c will add the logic');
  return { summary: 'noop' };
}
