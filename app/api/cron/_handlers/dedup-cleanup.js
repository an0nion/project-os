/**
 * Handler: dedup-cleanup
 * Stub — Unit 1b will add the deduplication-cleanup logic.
 *
 * Invoked by `/api/cron/daily/route.js` and `/api/cron/run/[name]/route.js`.
 * Not an HTTP route — pure async function.
 */

export async function run() {
  console.log('[dedup-cleanup] handler not yet wired — Unit 1b will add the logic');
  return { summary: 'noop' };
}
